import { RouteHandler } from '../ReactConstants';
import Header from './Header';

export const App = () => (
    <div>
        <Header/>
        <RouteHandler/>
    </div>
);

export default App;